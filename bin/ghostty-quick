#!/bin/bash
# ghostty-quick - Persistent zellij session for Ghostty Quick Terminal

SESSION="${GHOSTTY_SESSION:-dropdown}"

if zellij list-sessions 2>/dev/null | grep -q "$SESSION"; then
    exec zellij attach "$SESSION"
else
    exec zellij --session "$SESSION"
fi
